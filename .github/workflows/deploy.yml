name: Deploy to Cloudflare Pages

# Builds the static site and deploys it to Cloudflare Pages using an API
# token (no GitHub<->Cloudflare app link required). Runs on every push to
# main, once a day (to refresh the build-time Google review count), and
# on demand from the Actions tab.

on:
  push:
    branches: [main]
  schedule:
    - cron: "0 11 * * *" # daily at 11:00 UTC (~3-4 AM Pacific)
  workflow_dispatch: {}

concurrency:
  group: deploy-cloudflare-pages
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - run: npm ci

      - name: Build static site
        run: npm run build
        env:
          GOOGLE_PLACES_API_KEY: ${{ secrets.GOOGLE_PLACES_API_KEY }}
          GOOGLE_PLACE_ID: ${{ secrets.GOOGLE_PLACE_ID }}

      - name: Deploy to Cloudflare Pages
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
        run: |
          # Create the Pages project on first run; ignore the error if it already exists.
          npx --yes wrangler@3 pages project create royal-look --production-branch=main \
            || echo "Project already exists — continuing to deploy."
          npx --yes wrangler@3 pages deploy out --project-name=royal-look --branch=main --commit-dirty=true
