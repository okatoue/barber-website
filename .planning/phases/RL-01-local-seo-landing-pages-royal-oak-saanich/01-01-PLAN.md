---
phase: 01-local-seo-landing-pages-royal-oak-saanich
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/globals.css
  - src/lib/config.ts
  - src/lib/landing.ts
autonomous: true
requirements: [SEO-01, SEO-02, SEO-03, SEO-04, SEO-07]

must_haves:
  truths:
    - "The .btn-secondary class renders with a visible accent background (no longer an unstyled link)"
    - "SHOP exposes a Royal Oak Transit Exchange landmark string without altering the existing landmarks string"
    - "A typed LandingPageData array holds one unique entry per page (Royal Oak, Saanich) with intros that share no copy with each other or the homepage hero"
    - "Service prices are resolved from SERVICES in config, never hardcoded in the landing data"
  artifacts:
    - path: "src/app/globals.css"
      provides: ".btn-secondary definition near existing .btn rules"
      contains: ".btn-secondary"
    - path: "src/lib/config.ts"
      provides: "SHOP.landmarkRoyalOak field (Royal Oak Transit Exchange reference)"
      contains: "landmarkRoyalOak"
    - path: "src/lib/landing.ts"
      provides: "LandingPageData/EmphasizedService types, resolveService helper, ROYAL_OAK_DATA + SAANICH_DATA"
      exports: ["ROYAL_OAK_DATA", "SAANICH_DATA", "resolveService"]
      min_lines: 60
  key_links:
    - from: "src/lib/landing.ts"
      to: "src/lib/config.ts"
      via: "import { SERVICES, SHOP }"
      pattern: "from \"./config\"|from \"@/lib/config\""
    - from: "ROYAL_OAK_DATA.landmark"
      to: "SHOP.landmarkRoyalOak"
      via: "data references the new config field"
      pattern: "landmarkRoyalOak"
---

<objective>
Lay the foundation for the two landing pages: fix the undefined `.btn-secondary` design token, add the Royal Oak Transit Exchange landmark to config, and create the typed, config-driven data layer (`src/lib/landing.ts`) that holds the unique per-page copy and resolves prices from `SERVICES`.

Purpose: The component (Plan 02) and pages (Plan 03) consume these contracts. Defining them first prevents downstream scavenger-hunting and locks SEO-03 (unique intros) and SEO-04 (config-sourced prices) at the source.
Output: An accent-styled `.btn-secondary`, a `SHOP.landmarkRoyalOak` field, and `src/lib/landing.ts` exporting `ROYAL_OAK_DATA`, `SAANICH_DATA`, and `resolveService`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/RL-01-local-seo-landing-pages-royal-oak-saanich/01-CONTEXT.md
@.planning/phases/RL-01-local-seo-landing-pages-royal-oak-saanich/01-RESEARCH.md

<interfaces>
<!-- Contracts the executor needs. Extracted from the codebase — use directly, no exploration required. -->

Existing button rules in src/app/globals.css (lines 208–231) — mirror this structure for .btn-secondary:
```css
.btn { display: inline-flex; ...; border: 1px solid transparent; ... }
.btn-primary { background: var(--ink); color: var(--bg); border-color: var(--ink); }
.btn-primary:hover { background: var(--accent); border-color: var(--accent); }
.btn-ghost { background: transparent; color: var(--ink); border-color: var(--hairline-strong); }
```
Accent CSS variables available: --accent (gold), --accent-2, --bg, --ink.

Existing SHOP literal in src/lib/config.ts (ends with `} as const;` — adding a field just extends the inferred type, no annotation needed):
```typescript
landmarks: "Inside Broadmead Village Shopping Centre, just to the left of Starbucks — with free parking in the lot right out front.",
// ADD a new sibling field landmarkRoyalOak — do NOT change landmarks (consumed by LocationPreview.tsx)
```

SERVICES config names/prices/durations available for resolveService lookup (src/lib/config.ts):
| configName | price | duration |
|------------|-------|----------|
| Skin Fade | $30 | 45–60 min |
| Kids | $25 | 20–30 min |
| Regular Hair Cut | $28 | 30–45 min |
| Trim Beard | $20 | 20–30 min |
| Hot Shave | $35 | 30–40 min |

SERVICES shape: `SERVICES: { category: string; items: { name; duration; price; description? }[] }[]`.

Homepage hero copy that intros MUST NOT reuse any sentence from (src/components/Hero.tsx lede):
"Looking for a barber in Broadmead or Royal Oak? Royal Look Barber Shop offers precision haircuts, skin fades, beard trims, and traditional straight razor and hot towel shaves for men and kids across Saanich and Greater Victoria. Walk in any day, or call ahead to book your preferred barber — we'll get you looking sharp."
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define .btn-secondary in globals.css</name>
  <read_first>
    - src/app/globals.css (lines ~205–232, the .btn / .btn-primary / .btn-ghost block)
  </read_first>
  <files>src/app/globals.css</files>
  <action>
    Add a `.btn-secondary` rule and a `.btn-secondary:hover` rule immediately after the existing `.btn-ghost:hover` rule (around line 231), following the same property pattern as `.btn-primary`. Use the accent/gold variant: `background: var(--accent); color: var(--bg); border-color: var(--accent);` and on hover `background: var(--accent-2); border-color: var(--accent-2);`. This resolves the documented gap where `.btn-secondary` is referenced in three files but never defined (RESEARCH Pitfall 1). Do not modify `.btn`, `.btn-primary`, or `.btn-ghost`.
  </action>
  <verify>
    <automated>grep -q '\.btn-secondary' src/app/globals.css && grep -q '\.btn-secondary:hover' src/app/globals.css && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/globals.css` contains both a `.btn-secondary` selector and a `.btn-secondary:hover` selector.
    - The `.btn-secondary` rule sets `background`, `color`, and `border-color` using existing CSS variables (`var(--accent)`, `var(--bg)`).
    - `npm run build` exits 0 (no CSS/build regression).
  </acceptance_criteria>
  <done>`.btn-secondary` renders with an accent background instead of an unstyled link; existing button classes untouched; build green.</done>
</task>

<task type="auto">
  <name>Task 2: Add SHOP.landmarkRoyalOak field to config.ts</name>
  <read_first>
    - src/lib/config.ts (the SHOP object, especially the `landmarks` field and the `} as const;` close)
    - src/components/LocationPreview.tsx (line 56 — confirms `landmarks` is consumed here and must stay unchanged)
  </read_first>
  <files>src/lib/config.ts</files>
  <action>
    Add a new string field `landmarkRoyalOak` to the `SHOP` object as a sibling of the existing `landmarks` field. Value: a Royal Oak Transit Exchange reference, e.g. "Steps from Royal Oak Transit Exchange (BC Transit hub on Royal Oak Drive) — and right inside Broadmead Village Shopping Centre." Keep the existing `landmarks` string byte-for-byte unchanged (it is consumed by `LocationPreview.tsx`; mutating it is forbidden per CONTEXT). Do not add an explicit type annotation — `SHOP` is `as const`, so adding the literal field extends the inferred type automatically.
  </action>
  <verify>
    <automated>grep -q 'landmarkRoyalOak' src/lib/config.ts && grep -q 'Royal Oak Transit Exchange' src/lib/config.ts && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/config.ts` contains a `landmarkRoyalOak` field whose value includes the literal "Royal Oak Transit Exchange".
    - The original `landmarks` string is unchanged (still references Starbucks + free parking).
    - `npm run build` exits 0 with no TypeScript error.
  </acceptance_criteria>
  <done>`SHOP.landmarkRoyalOak` is available to consumers; `SHOP.landmarks` is untouched; build/type-check green.</done>
</task>

<task type="auto">
  <name>Task 3: Create src/lib/landing.ts (types, resolveService, page data)</name>
  <read_first>
    - src/lib/config.ts (SERVICES + SHOP, including the new landmarkRoyalOak field from Task 2)
    - src/app/services/page.tsx (SERVICE_DETAILS tone — neighbourhood-warm, factual, no hype)
    - src/components/Hero.tsx (homepage hero lede — intros must share NO sentence with it)
    - .planning/phases/RL-01-local-seo-landing-pages-royal-oak-saanich/01-RESEARCH.md (Pattern 6 data shape; Pitfall 7 doorway-page guidance)
  </read_first>
  <files>src/lib/landing.ts</files>
  <action>
    Create `src/lib/landing.ts`. Export an `EmphasizedService` type (`displayName`, `configName`, `description`) and a `LandingPageData` type (`slug`, `metaTitle`, `metaDescription`, `eyebrow`, `h1`, `h1Emphasis`, `intro`, `emphasizedServices: EmphasizedService[]`, `landmark`, `callLocation`, `callLocationPrimary`, `breadcrumbLabel`, `areaServedName`). Export a `resolveService(configName: string)` helper that scans `SERVICES` categories' `items` for a matching `name` and returns the `Service` (or null) — this is the single source for price/duration; never hardcode prices in the data.

    Export `ROYAL_OAK_DATA`: slug `"royal-oak-barber-shop"`, h1 `"Royal Oak Barber Shop"` with h1Emphasis `"Barber Shop."`, eyebrow `"Royal Oak · Victoria, BC"`, landmark set to `SHOP.landmarkRoyalOak`, callLocation `"royal_oak_page_cta"`, callLocationPrimary `"royal_oak_page_cta_primary"`, breadcrumbLabel `"Royal Oak Barber Shop"`, areaServedName `"Royal Oak, Victoria BC"`. emphasizedServices: Skin Fade (configName `"Skin Fade"`), Kids (configName `"Kids"`, displayName `"Kids' Haircut"`), plus a supporting Regular Hair Cut (configName `"Regular Hair Cut"`). Write a unique intro of 200–250 words emphasizing skin fades and kids' cuts, naming the Royal Oak Transit Exchange and Broadmead Village Shopping Centre, with the exact phrase "Royal Oak Barber Shop" in the first 50 words.

    Export `SAANICH_DATA`: slug `"beard-trim-saanich"`, h1 `"Beard Trim in Saanich"` with h1Emphasis `"in Saanich."`, eyebrow `"Saanich · Victoria, BC"`, landmark `SHOP.landmarks` (the existing Broadmead/parking string), callLocation `"saanich_beard_page_cta"`, callLocationPrimary `"saanich_beard_page_cta_primary"`, breadcrumbLabel `"Beard Trim in Saanich"`, areaServedName `"Saanich, Victoria BC"`. emphasizedServices: Trim Beard (configName `"Trim Beard"`, displayName `"Beard Trim"`), Hot Shave (configName `"Hot Shave"`, displayName `"Hot-Towel Straight-Razor Shave"`), plus supporting Regular Hair Cut. Write a unique 200–250 word intro emphasizing beard trims and hot-towel / straight-razor shaves, referencing Saanich plus the Broadmead/Royal Oak neighbourhood context, with the exact phrase "Beard Trim in Saanich" in the first 50 words.

    Both intros must share no sentence with each other or with the Hero lede (avoid doorway/thin-content). Use a different opening sentence for each. The `displayName` may be friendlier than the config key (e.g. "Beard Trim" displayed while `configName` stays "Trim Beard") — do NOT rename config keys (that is Phase 3).
  </action>
  <verify>
    <automated>grep -q 'ROYAL_OAK_DATA' src/lib/landing.ts && grep -q 'SAANICH_DATA' src/lib/landing.ts && grep -q 'resolveService' src/lib/landing.ts && grep -q 'Royal Oak Transit Exchange' src/lib/landing.ts && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/landing.ts` exports `ROYAL_OAK_DATA`, `SAANICH_DATA`, `resolveService`, and the `LandingPageData` / `EmphasizedService` types.
    - `resolveService` reads `SERVICES` from config (grep `from "./config"` or `from "@/lib/config"`); no `$`-prefixed price literals appear in the data entries.
    - Royal Oak entry references "Royal Oak Transit Exchange"; its `landmark` is `SHOP.landmarkRoyalOak`. Saanich entry's `landmark` is `SHOP.landmarks`.
    - `ROYAL_OAK_DATA.callLocation` === `"royal_oak_page_cta"` and `SAANICH_DATA.callLocation` === `"saanich_beard_page_cta"`.
    - `npm run build` exits 0 (file type-checks). Word-count and uniqueness are sign-off-gated in Plan 04; here each intro must be visibly ≥150 words.
  </acceptance_criteria>
  <done>`landing.ts` provides the typed, config-sourced data contract with two unique intros; downstream plans can import it without modification.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build-time source → static export | All values in this plan are author-controlled constants compiled into static HTML/CSS. No runtime, no user input, no network ingress. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-01 | Tampering | src/lib/landing.ts / config.ts | accept | Values are static build-time constants; no user input reaches them. No injection surface. |
| T-1-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase (RESEARCH "No new packages required"); supply-chain surface unchanged. |

This plan adds presentational data and a CSS rule only — realistic threat surface is near-zero (confirmed by RESEARCH Security Domain section). No ASVS L1 control applies.
</threat_model>

<verification>
- `npm run build` exits 0 after all three tasks.
- `grep -q '\.btn-secondary' src/app/globals.css` passes.
- `grep -q 'landmarkRoyalOak' src/lib/config.ts` passes and `SHOP.landmarks` is unchanged.
- `grep -q 'ROYAL_OAK_DATA' src/lib/landing.ts` and `grep -q 'SAANICH_DATA' src/lib/landing.ts` pass.
</verification>

<success_criteria>
- `.btn-secondary` defined with accent styling; existing buttons untouched.
- `SHOP.landmarkRoyalOak` added; `SHOP.landmarks` preserved.
- `src/lib/landing.ts` exports the two data entries, the types, and `resolveService`; prices flow from config; intros are unique and locally specific.
- Build is green.
</success_criteria>

<output>
Create `.planning/phases/RL-01-local-seo-landing-pages-royal-oak-saanich/01-01-SUMMARY.md` when done.
</output>
