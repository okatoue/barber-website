---
phase: 01-local-seo-landing-pages-royal-oak-saanich
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - src/components/LandingPage.tsx
autonomous: true
requirements: [SEO-04, SEO-05, SEO-06, SEO-07]

must_haves:
  truths:
    - "One reusable presentational component renders any LandingPageData entry — header, unique intro, emphasized services, map, NAP, hours, and a Call CTA"
    - "Each emphasized service shows a price pulled from SERVICES via resolveService (not a hardcoded literal)"
    - "The Google Map iframe, full NAP, and tel: Call CTAs with per-page data-call-location render on the page"
    - "The component emits a single JSON-LD @graph (Service entities + BreadcrumbList) whose provider references the sitewide #barbershop node by @id only"
  artifacts:
    - path: "src/components/LandingPage.tsx"
      provides: "Reusable Server Component for landing pages (UI + JSON-LD)"
      exports: ["default"]
      min_lines: 90
      contains: "application/ld+json"
  key_links:
    - from: "src/components/LandingPage.tsx"
      to: "src/lib/landing.ts"
      via: "import { resolveService } + LandingPageData type"
      pattern: "from \"@/lib/landing\""
    - from: "src/components/LandingPage.tsx"
      to: "SHOP.googleMapsEmbed"
      via: "iframe src"
      pattern: "googleMapsEmbed"
    - from: "Service JSON-LD provider"
      to: "layout.tsx #barbershop node"
      via: "@id reference (no full node re-emission)"
      pattern: "#barbershop"
---

<objective>
Build `src/components/LandingPage.tsx` — the single reusable, data-driven Server Component that renders both landing pages from a `LandingPageData` entry: header + unique intro, emphasized services (prices resolved from config), the Google Map + NAP + hours block, a `tel:` Call CTA with per-page `data-call-location`, and a per-page JSON-LD `@graph`.

Purpose: Satisfies SEO-07 (one reusable component, correct design classes), SEO-05 (map/NAP/CTA), SEO-04 (config-sourced prices), and SEO-06 (Service JSON-LD). The two thin route files in Plan 03 only pass data into this component.
Output: `src/components/LandingPage.tsx` (Server Component, no `"use client"`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/RL-01-local-seo-landing-pages-royal-oak-saanich/01-CONTEXT.md
@.planning/phases/RL-01-local-seo-landing-pages-royal-oak-saanich/01-RESEARCH.md

<interfaces>
<!-- Contracts from Plan 01 + the codebase. Use directly. -->

From src/lib/landing.ts (created in Plan 01-01):
```typescript
export type EmphasizedService = { displayName: string; configName: string; description: string };
export type LandingPageData = {
  slug; metaTitle; metaDescription; eyebrow; h1; h1Emphasis; intro;
  emphasizedServices: EmphasizedService[];
  landmark; callLocation; callLocationPrimary; breadcrumbLabel; areaServedName;
};
export function resolveService(configName: string): { name; duration; price } | null;
export const ROYAL_OAK_DATA: LandingPageData;
export const SAANICH_DATA: LandingPageData;
```

From src/lib/config.ts: SHOP.name, SHOP.address.{street,city,province,postal}, SHOP.phone, SHOP.hours[{day,open,close}], SHOP.googleMapsEmbed, SHOP.siteUrl.

Design-class structure to MIRROR — src/app/services/page.tsx (header + services):
```tsx
<section className="section"><div className="container">
  <div className="section-head"><div>
    <div className="eyebrow">{eyebrow}</div>
    <h1 className="serif">Royal Oak <em>Barber Shop.</em></h1>
  </div><p className="lede">{tagline-or-intro-lead}</p></div>
  <div className="menu-col"><div className="menu-row">
    <div><div className="nm">{name}</div><div className="dur">{duration}</div></div>
    <span className="pr serif">{price}</span>
  </div></div>
</div></section>
```

Map + NAP block to MIRROR — src/components/LocationPreview.tsx (find-grid/find-map/find-info, iframe, addr, hours-grid, tel: with data-call-location). The tel href pattern: `tel:${SHOP.phone.replace(/\D/g, "")}`, link color `var(--accent)`.

JSON-LD injection pattern to MIRROR — src/app/layout.tsx lines 67–121 (Server Component `<script type="application/ld+json" dangerouslySetInnerHTML={{ __html: JSON.stringify(...) }} />`). Sitewide node already emitted there is `{ "@type": "BarberShop", "@id": "${SHOP.siteUrl}/#barbershop", ... }`.

Call tracking — src/components/Analytics.tsx already has a delegated document click listener firing `call_click` for ANY `a[href^="tel:"]` carrying `data-call-location`. No Analytics change needed; just add the attribute.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build LandingPage.tsx visible UI (header, intro, services, map, NAP, CTA)</name>
  <read_first>
    - src/app/services/page.tsx (section/container/section-head/eyebrow/serif/lede + menu-col/menu-row/nm/dur/pr structure)
    - src/components/LocationPreview.tsx (find-grid/find-map/find-info, iframe, addr, hours-grid, tel: CTA)
    - src/lib/landing.ts (LandingPageData type + resolveService — from Plan 01-01)
    - src/lib/config.ts (SHOP fields)
    - src/app/globals.css (confirm class names: section, container, section-head, eyebrow, serif, lede, menu-col, menu-row, find-grid, find-map, find-info, addr, hours-grid, find-note, btn, btn-secondary)
  </read_first>
  <files>src/components/LandingPage.tsx</files>
  <action>
    Create `src/components/LandingPage.tsx` as a Server Component (NO `"use client"`) accepting a single prop `{ data }: { data: LandingPageData }`. Import `SHOP` from `@/lib/config` and `resolveService` + the `LandingPageData` type from `@/lib/landing`. Mirror `services/page.tsx` (NOT `location/page.tsx`, which uses undefined classes/stale tokens per RESEARCH Pitfall 3).

    Render a first `<section className="section"><div className="container">` block: a `section-head` with `<div className="eyebrow">{data.eyebrow}</div>` and an `<h1 className="serif">` rendering `data.h1` text with the `data.h1Emphasis` portion wrapped in `<em>` (the `em` gets italic gold automatically). Below it, render the unique `data.intro` in a constrained paragraph (e.g. `maxWidth: "70ch"`, `color: var(--ink-2)` or `var(--muted)`). Then render emphasized services using the `menu-col` / `menu-row` pattern: for each `data.emphasizedServices` entry, call `resolveService(svc.configName)` and display `svc.displayName` as `.nm`, the resolved `duration` as `.dur`, the resolved `price` as `.pr serif`, and `svc.description` as supporting copy. Prices MUST come from `resolveService` (config) — never hardcode `$30`/`$20`/etc.

    Render a second `<section className="section"><div className="container">` with a `find-grid` containing a `find-map` (the `SHOP.googleMapsEmbed` iframe — copy the iframe attributes verbatim from LocationPreview: width/height 100%, `loading="lazy"`, `referrerPolicy="no-referrer-when-downgrade"`, `allowFullScreen`, a `title`) and a `find-info` block: `<h3 className="serif">{SHOP.name}</h3>`, the full NAP address (street, city/province/postal) using `SHOP.address.*`, a `tel:` link `href={`tel:${SHOP.phone.replace(/\D/g, "")}`}` carrying `data-call-location={data.callLocation}`, a `<p className="find-note">{data.landmark}</p>`, the `hours-grid` mapping `SHOP.hours` (mirror LocationPreview lines 66–81), and a primary Call CTA `<a className="btn btn-secondary" href={`tel:...`} data-call-location={data.callLocationPrimary}>Call {SHOP.phone}</a>`. (`OpenStatus` pill is optional — Claude's discretion.)
  </action>
  <verify>
    <automated>grep -q 'data-call-location' src/components/LandingPage.tsx && grep -q 'googleMapsEmbed' src/components/LandingPage.tsx && grep -q 'resolveService' src/components/LandingPage.tsx && grep -q 'find-grid' src/components/LandingPage.tsx && grep -Lq 'use client' src/components/LandingPage.tsx; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/LandingPage.tsx` exists, has NO `"use client"` directive, and exports a default component taking `{ data }`.
    - File contains class names `section`, `container`, `section-head`, `eyebrow`, `serif`, `lede` (or constrained intro), `menu-col`, `menu-row`, `find-grid`, `find-map`, `find-info` — and NONE of the stale tokens `text-gold`, `bg-surface`, `border-border`, `section-heading`, `className="card"`.
    - Prices are produced by `resolveService(...)`; no `$`-prefixed price literals are hardcoded in the JSX.
    - At least one `tel:` link carries `data-call-location`; the iframe `src` is `SHOP.googleMapsEmbed`; the Call CTA uses class `btn btn-secondary`.
    - `npm run build` exits 0.
  </acceptance_criteria>
  <done>The component renders a complete landing page body (header, intro, config-priced services, map, NAP, hours, Call CTA) from any LandingPageData entry; build green.</done>
</task>

<task type="auto">
  <name>Task 2: Emit per-page JSON-LD @graph (Service entities + BreadcrumbList)</name>
  <read_first>
    - src/app/layout.tsx (lines 67–121 — the sitewide BarberShop JSON-LD script + the `@id: ${SHOP.siteUrl}/#barbershop` node; copy the dangerouslySetInnerHTML pattern)
    - src/components/LandingPage.tsx (the component from Task 1, where the script tag is added)
    - .planning/phases/RL-01-local-seo-landing-pages-royal-oak-saanich/01-RESEARCH.md (JSON-LD Specification section + Pitfall 4 duplicate @id)
  </read_first>
  <files>src/components/LandingPage.tsx</files>
  <action>
    At the top of the component's returned JSX (wrap the existing sections in a fragment), add a `<script type="application/ld+json" dangerouslySetInnerHTML={{ __html: JSON.stringify(graph) }} />` exactly like `layout.tsx`. Build `graph` as `{ "@context": "https://schema.org", "@graph": [...] }`.

    For each entry in `data.emphasizedServices`, push a `Service` node: `@type` `"Service"`, `name` composed from `svc.displayName` + brand (e.g. `${svc.displayName} — Royal Look Barber Shop`), `serviceType` `svc.displayName`, `description` `svc.description`, `areaServed` `{ "@type": "Place", "name": data.areaServedName }`, `provider` `{ "@id": `${SHOP.siteUrl}/#barbershop` }` (ID POINTER ONLY — do NOT include `@type`/`name`/`address` in provider; do NOT emit a second full BarberShop node — RESEARCH Pitfall 4), and `offers` `{ "@type": "Offer", "priceCurrency": "CAD", "price": <numeric> }` where `<numeric>` is the `resolveService(svc.configName).price` with the leading `$` stripped (e.g. "$30" → "30").

    Append a `BreadcrumbList` node with two `ListItem`s: position 1 Home → `SHOP.siteUrl`; position 2 `data.breadcrumbLabel` → `${SHOP.siteUrl}/${data.slug}`. Because this is a Server Component, `dangerouslySetInnerHTML` is valid and renders into the static HTML at build time.
  </action>
  <verify>
    <automated>grep -q 'application/ld+json' src/components/LandingPage.tsx && grep -q '@graph' src/components/LandingPage.tsx && grep -q '#barbershop' src/components/LandingPage.tsx && grep -q 'BreadcrumbList' src/components/LandingPage.tsx && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/LandingPage.tsx` contains a `<script type="application/ld+json">` emitting an `@graph` with one or more `Service` nodes and a `BreadcrumbList`.
    - Each `Service.provider` is an `@id` reference to `${SHOP.siteUrl}/#barbershop` and the component does NOT emit a `"@type": "BarberShop"` node (no duplicate of the layout node).
    - `Service.offers.price` is a bare number string derived from config (no `$`), `priceCurrency` is `"CAD"`.
    - `npm run build` exits 0; external JSON-LD validation is sign-off-gated in Plan 04.
  </acceptance_criteria>
  <done>Each rendered page carries a valid Service/Breadcrumb @graph that references the sitewide BarberShop by @id without conflict; build green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → static page | Page is read-only static HTML; no inputs accepted. |
| page → Google Maps iframe | The only external embed; served from Google's own domain via `SHOP.googleMapsEmbed`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-02 | Injection (XSS) | dangerouslySetInnerHTML JSON-LD | accept | `JSON.stringify` of author-controlled constants only — no user/runtime input flows into the script. Same pattern already shipped in `layout.tsx`. |
| T-1-03 | Information disclosure | Google Maps iframe | accept | Embed is Google-hosted; `referrerPolicy="no-referrer-when-downgrade"` matches existing site behavior; no PII rendered. |
| T-1-SC | Tampering | npm/pip/cargo installs | accept | No package installs this phase; supply-chain surface unchanged. |

Near-zero realistic surface for static presentational pages (RESEARCH Security Domain). No ASVS L1 control triggered.
</threat_model>

<verification>
- `npm run build` exits 0.
- `grep -q 'use client' src/components/LandingPage.tsx` returns NO match (must be a Server Component).
- `grep -q 'application/ld+json' src/components/LandingPage.tsx` and `grep -q '#barbershop' src/components/LandingPage.tsx` pass.
- `grep -q 'find-grid' && grep -q 'menu-row'` pass (correct design classes).
</verification>

<success_criteria>
- One reusable Server Component renders header, unique intro, config-priced services, map, NAP, hours, and a tracked Call CTA from a `LandingPageData` prop.
- Per-page JSON-LD `@graph` emits Service nodes + a BreadcrumbList, referencing `#barbershop` by `@id` only.
- Build is green.
</success_criteria>

<output>
Create `.planning/phases/RL-01-local-seo-landing-pages-royal-oak-saanich/01-02-SUMMARY.md` when done.
</output>
