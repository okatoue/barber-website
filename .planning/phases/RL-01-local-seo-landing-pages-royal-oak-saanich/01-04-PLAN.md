---
phase: 01-local-seo-landing-pages-royal-oak-saanich
plan: 04
type: execute
wave: 4
depends_on: [01-03]
files_modified: []
autonomous: false
requirements: [SEO-06, SEO-08]

must_haves:
  truths:
    - "npm run build completes a clean static export including both new routes"
    - "All per-requirement build-output assertions (SEO-01..SEO-08) pass over out/ HTML and out/sitemap.xml"
    - "Each page's JSON-LD validates with no errors in an external validator and references #barbershop with no duplicate node"
    - "Both intros read as genuinely unique, locally specific, ≥150 words, and non-spammy"
  artifacts: []
  key_links:
    - from: "out/royal-oak-barber-shop/index.html"
      to: "schema.org Service @graph"
      via: "application/ld+json script validated externally"
      pattern: "application/ld\\+json"
---

<objective>
Final phase gate: run the full static build and assert every Phase 1 requirement against the generated `out/` output, then collect human sign-off on the two manual-only checks the build cannot decide — external JSON-LD validity and intro uniqueness/quality.

Purpose: This is the validation contract from `01-VALIDATION.md` ("Before /gsd:verify-work: full build green + all requirement assertions pass + JSON-LD validated in Google Rich Results Test"). It closes SEO-08 (clean export) and the JSON-LD half of SEO-06, and guards SEO-03 against thin/doorway content.
Output: A green full build, passing assertion log, and recorded human approval (no source files changed).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/RL-01-local-seo-landing-pages-royal-oak-saanich/01-VALIDATION.md
@.planning/phases/RL-01-local-seo-landing-pages-royal-oak-saanich/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full build + per-requirement assertions over out/</name>
  <read_first>
    - .planning/phases/RL-01-local-seo-landing-pages-royal-oak-saanich/01-VALIDATION.md (Per-Requirement Verification Map — the authoritative assertion list)
    - src/lib/config.ts (confirm expected price/NAP literals: Skin Fade $30, Kids $25, Trim Beard $20, Hot Shave $35, address "777 Royal Oak Dr")
  </read_first>
  <files>(no source changes — verification only)</files>
  <action>
    Run `npm run build` from the repo root and confirm it exits 0 (clean static export to `out/`). Then run the requirement assertions from `01-VALIDATION.md` against the generated output and record pass/fail for each:
    - SEO-01: `out/royal-oak-barber-shop/index.html` contains `<h1` … `Royal Oak Barber Shop`, plus `Royal Oak Transit Exchange`, `skin`, and `kids`.
    - SEO-02: `out/beard-trim-saanich/index.html` contains `<h1` … `Beard Trim in Saanich`, plus `beard` and (`hot` or `straight razor`).
    - SEO-04: each page's HTML shows the config prices (Royal Oak: `$30` Skin Fade, `$25` Kids; Saanich: `$20` Trim Beard, `$35` Hot Shave).
    - SEO-05: each `index.html` contains `maps.google.com/maps?q=`, `777 Royal Oak Dr`, `href="tel:`, and a `data-call-location` value (`royal_oak_page_cta`, `saanich_beard_page_cta`).
    - SEO-06: each `index.html` contains `<link rel="canonical" href="https://royallook.ca/<slug>"` and a `<script type="application/ld+json">` containing `#barbershop` (referenced, not re-declared as a BarberShop node).
    - SEO-07: each `index.html` contains design classes `section`, `container`, `section-head`, `find-grid`, `find-map`, `find-info`, and NONE of the stale tokens `text-gold`/`bg-surface`/`border-border`.
    - SEO-08: `out/sitemap.xml` contains both `royallook.ca/royal-oak-barber-shop` and `royallook.ca/beard-trim-saanich`.
    Report any failing assertion as a blocker (do not approve the phase with a red assertion).
  </action>
  <verify>
    <automated>npm run build && grep -q 'Royal Oak Barber Shop' out/royal-oak-barber-shop/index.html && grep -q 'Royal Oak Transit Exchange' out/royal-oak-barber-shop/index.html && grep -q 'Beard Trim in Saanich' out/beard-trim-saanich/index.html && grep -q 'rel="canonical" href="https://royallook.ca/royal-oak-barber-shop"' out/royal-oak-barber-shop/index.html && grep -q 'rel="canonical" href="https://royallook.ca/beard-trim-saanich"' out/beard-trim-saanich/index.html && grep -q '#barbershop' out/royal-oak-barber-shop/index.html && grep -q 'data-call-location' out/royal-oak-barber-shop/index.html && grep -q 'royallook.ca/royal-oak-barber-shop' out/sitemap.xml && grep -q 'royallook.ca/beard-trim-saanich' out/sitemap.xml</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0.
    - Every SEO-01..SEO-08 assertion above passes against the generated `out/` files.
    - Both canonicals resolve to absolute `https://royallook.ca/<slug>` in the built HTML.
    - `out/sitemap.xml` contains both new URLs.
  </acceptance_criteria>
  <done>Full static export is clean and every greppable requirement assertion passes; any failure is surfaced as a blocker before sign-off.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human sign-off — external JSON-LD validity + intro uniqueness/quality</name>
  <read_first>
    - out/royal-oak-barber-shop/index.html (the `<script type="application/ld+json">` block to validate)
    - out/beard-trim-saanich/index.html (same)
    - src/lib/landing.ts (the two intro strings)
    - src/components/Hero.tsx (homepage hero lede — intros must share no sentence with it)
    - .planning/phases/RL-01-local-seo-landing-pages-royal-oak-saanich/01-VALIDATION.md (Manual-Only Verifications table)
  </read_first>
  <files>(no source changes — manual sign-off only)</files>
  <what-built>
    Two static landing pages built to `out/royal-oak-barber-shop/index.html` and `out/beard-trim-saanich/index.html`, each emitting a Service/BreadcrumbList JSON-LD `@graph` and a unique ≥150-word intro. The greppable assertions (Task 1) already passed; these two checks require human/external judgment per `01-VALIDATION.md` Manual-Only Verifications.
  </what-built>
  <action>
    Present the two manual-only checks to the user and collect approval (this task writes no source). (1) JSON-LD validity (SEO-06): from each built HTML copy the `<script type="application/ld+json">` contents and paste into https://validator.schema.org (optionally Google Rich Results Test); expect zero errors, `Service` nodes present, `provider` an `@id` reference to `https://royallook.ca/#barbershop`, and NO second `BarberShop` node duplicating that `@id`. (2) Intro uniqueness + quality (SEO-03): read both intros from `src/lib/landing.ts`, confirm each is ≥150 words (e.g. `wc -w` on the extracted string), that the two intros share no sentence with each other or with the homepage hero lede in `src/components/Hero.tsx`, that each opens with a distinct sentence, and that the copy reads as natural local content (not keyword-stuffed / doorway). If either check fails, record the specific JSON-LD error or the intro needing rewrite so a gap-closure pass can fix it rather than approving.
  </action>
  <how-to-verify>
    1. JSON-LD validity (SEO-06): Open each built HTML, copy the `<script type="application/ld+json">…</script>` contents, and paste into https://validator.schema.org (and optionally Google Rich Results Test). Expect: no errors; `Service` nodes present; `provider` is an `@id` reference to `https://royallook.ca/#barbershop`; NO second `BarberShop` node duplicating that `@id`.
    2. Intro uniqueness + quality (SEO-03): Read both intros (in `src/lib/landing.ts` or the rendered pages). Confirm each is ≥150 words (run a word count, e.g. paste into a counter or `wc -w`), the two intros share no sentence with each other or with the homepage hero lede (`src/components/Hero.tsx`), each has a distinct opening sentence, and the copy reads as natural local content (not keyword-stuffed / doorway).
    Approve only if both checks pass; otherwise describe the specific JSON-LD error or the intro that needs a rewrite so a gap-closure pass can fix it.
  </how-to-verify>
  <verify>
    <human-check>External schema validators report zero errors for both pages' JSON-LD (provider is an `@id` pointer to `#barbershop`, no duplicate BarberShop node), AND both intros are confirmed ≥150 words and share no sentence with each other or the homepage hero. Automated build-output assertions are covered by Task 1.</human-check>
  </verify>
  <acceptance_criteria>
    - External validator reports zero errors for each page's JSON-LD.
    - `provider` is an `@id` pointer to `#barbershop`; no duplicate BarberShop node.
    - Each intro is ≥150 words, shares no sentence with the other intro or the homepage hero, and reads as genuine local content.
    - Human approval recorded (or specific issues listed for follow-up).
  </acceptance_criteria>
  <done>Both pages' JSON-LD validate cleanly in an external validator and both intros are confirmed unique and ≥150 words; user approval recorded (or issues logged for gap closure).</done>
  <resume-signal>Type "approved" once both JSON-LD validators are clean and both intros are confirmed unique and ≥150 words, or describe the issues to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| verification only | This plan changes no source; it inspects build output and consults an external schema validator. No new attack surface introduced. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-05 | (validation gate) | JSON-LD external validation | mitigate | Human checkpoint blocks phase completion until schema.org/Rich Results validation is clean — catches any malformed structured data before it ships. |
| T-1-SC | Tampering | npm/pip/cargo installs | accept | No package installs this phase; supply-chain surface unchanged. |

No code is added; the only "control" here is the verification gate itself. Realistic threat surface remains near-zero (RESEARCH Security Domain).
</threat_model>

<verification>
- `npm run build` exits 0.
- All SEO-01..SEO-08 build-output assertions pass.
- Both pages' JSON-LD validate externally with no errors and reference `#barbershop` by `@id` only.
- Both intros confirmed unique and ≥150 words by human read.
</verification>

<success_criteria>
- Clean static export including both new routes.
- Every Phase 1 requirement assertion passes (automated) and both manual-only checks are signed off (human).
- Phase 1 is ready for `/gsd:verify-work`.
</success_criteria>

<output>
Create `.planning/phases/RL-01-local-seo-landing-pages-royal-oak-saanich/01-04-SUMMARY.md` when done.
</output>
