---
phase: 02-faq-page-with-faqpage-schema
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/lib/config.ts]
autonomous: true
requirements: [FAQ-01, FAQ-03]

must_haves:
  truths:
    - "FAQ_ITEMS covers all seven BrightLocal topics with genuinely useful 1-3 sentence answers (walk-ins, kids' cuts, skin-fade technique, beard-trim options, straight-razor shave hygiene, parking, typical timing/pricing)"
    - "Pricing/timing answers derive their figures from SERVICES at build time (no hardcoded dollar/duration literals inside FAQ answer strings) (D-04)"
    - "Each item carries a category, and the homepage curated subset is selected from the same single source via a homepage flag (D-06)"
  artifacts:
    - path: "src/lib/config.ts"
      provides: "Expanded, typed FAQ_ITEMS (>=7 topics) with category + homepage flag, plus the FaqItem type"
      contains: "FaqItem"
    - path: "src/lib/config.ts"
      provides: "Homepage subset markers (>=3 items flagged for the homepage teaser: walk-ins, kids, timing/pricing)"
      contains: "homepage"
  key_links:
    - from: "src/lib/config.ts FAQ_ITEMS pricing answers"
      to: "SERVICES"
      via: "lookup + template-literal interpolation"
      pattern: "SERVICES"
---

<objective>
Expand the config-driven FAQ data model in `src/lib/config.ts` so it is the single source of truth for both `/faq` (full set) and the homepage teaser (curated subset). Add a `FaqItem` type (question, answer, category, optional `homepage` flag), grow `FAQ_ITEMS` to cover all seven BrightLocal topics with warm `en_CA` answers, and make every pricing/timing answer pull its figures from `SERVICES` instead of hardcoding them.

Purpose: This is the foundation the whole phase builds on — `/faq` (02-02) and the homepage teaser (02-03) both consume this. Implements D-04 (prices from SERVICES), D-06 (single-source data model, Option A), and the content half of FAQ-01 and FAQ-03.
Output: A typed, expanded `FAQ_ITEMS` and `FaqItem` type in `config.ts`; no other files touched.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/RL-02-faq-page-with-faqpage-schema/02-CONTEXT.md
@.planning/phases/RL-02-faq-page-with-faqpage-schema/02-PATTERNS.md
@src/lib/config.ts
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add FaqItem type, a SERVICES price lookup, and restructure the existing items</name>
  <read_first>
    - src/lib/config.ts (the file being modified — current `FAQ_ITEMS` at ~lines 176-202, the `Service`/`ServiceCategory` shapes at ~lines 64-127, and `SERVICES` values at ~lines 76-127)
    - .planning/phases/RL-02-faq-page-with-faqpage-schema/02-CONTEXT.md (D-04 prices-from-SERVICES, D-06 data model, Claude's Discretion on category names)
    - .planning/phases/RL-02-faq-page-with-faqpage-schema/02-PATTERNS.md (config.ts section — Option A recommended: single array with `category` + optional `homepage` flag; SERVICES key values table)
  </read_first>
  <files>src/lib/config.ts</files>
  <action>
    In the FAQ section of `config.ts`, add an exported `FaqItem` type with fields `question: string`, `answer: string`, `category: string`, and `homepage?: boolean` (Option A from PATTERNS / D-06 — single array, single source of truth). Annotate `FAQ_ITEMS` as `FaqItem[]`.

    Add a small SERVICES lookup helper so pricing/timing answers can interpolate live figures instead of hardcoding them (D-04). For example a `findService(name)` that flattens `SERVICES` items and returns the match by `name`, exposing `.price` and `.duration`. Use the exact SERVICES item names: `"Regular Hair Cut"` ($28, 30-45 min), `"Skin Fade"` ($30, 45-60 min), `"Kids"` ($25, 20-30 min), `"Trim Beard"` ($20, 20-30 min), `"Hot Shave"` ($35, 30-40 min).

    Convert the FIVE existing items into the new shape, assigning a `category` to each (category names are your discretion — e.g. "Visiting", "Services", "Kids" or "Pricing") and a `homepage: true` flag on the conversion-focused ones per D-02:
    - "Do you take walk-ins?" -> category "Visiting", `homepage: true`.
    - "How long does a haircut take?" -> this is the timing/pricing topic: rewrite so the durations/prices are interpolated from `findService(...)` (Regular Hair Cut, Skin Fade) rather than the current literals; category "Visiting" or "Pricing", `homepage: true`.
    - "Do you cut kids' hair?" -> replace the hardcoded "start at $25" with the interpolated `findService("Kids").price`; category "Kids", `homepage: true`.
    - "What's the difference between a fade and a taper?" -> category "Services", no homepage flag (bonus item).
    - "What's your cancellation policy?" -> category "Visiting", no homepage flag (bonus item).
    Keep the existing warm, neighbourhood, no-hype `en_CA` voice. Do NOT touch `SERVICES`, `SHOP`, `BARBERS`, `NAV_LINKS_*`, or any non-FAQ export.
  </action>
  <acceptance_criteria>
    - `src/lib/config.ts` exports a `FaqItem` type with exactly the fields `question`, `answer`, `category`, and optional `homepage`.
    - `FAQ_ITEMS` is annotated `FaqItem[]` and every item has a non-empty `category`.
    - At least three items carry `homepage: true` (must include the walk-ins, kids, and timing/pricing items per D-02).
    - The kids and timing answers no longer contain hardcoded price literals; they interpolate values via the SERVICES lookup (a scoped read of those answer strings shows no `$NN` literal — the figures come from `SERVICES`).
    - `npx tsc --noEmit` reports no type errors.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>The FAQ data model is typed (`FaqItem`), the five legacy items are categorized + subset-flagged, and pricing answers read from SERVICES.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Author the four net-new BrightLocal topic answers</name>
  <read_first>
    - src/lib/config.ts (after Task 1 — the new `FaqItem` shape, `findService` helper, and `SERVICES` values to cite)
    - .planning/phases/RL-02-faq-page-with-faqpage-schema/02-CONTEXT.md (`<specifics>` — the seven topics and the warm/neighbourhood `en_CA` voice; SHOP.landmarks parking copy)
    - src/app/services/page.tsx (SERVICE_DETAILS copy — match this tone/voice for the new answers)
  </read_first>
  <files>src/lib/config.ts</files>
  <action>
    Add the four topics not yet covered so `FAQ_ITEMS` answers ALL seven BrightLocal topics (FAQ-01), each a genuinely useful 1-3 sentence answer in the established voice:
    - Skin-fade technique: blended by hand low/mid/high down to the skin; cite Skin Fade price/duration via `findService("Skin Fade")`. category "Services".
    - Beard-trim options: shaping + clean lineup along cheeks/neck; cite Trim Beard via `findService("Trim Beard")`, and mention the Hot Towel / straight-razor option. category "Services".
    - Straight-razor shave hygiene: emphasize a fresh single-use blade per client and a clean hot-towel setup; cite Hot Shave via `findService("Hot Shave")`. category "Services".
    - Parking: free parking in the lot right out front, inside Broadmead Village Shopping Centre just left of Starbucks — reuse the wording already in `SHOP.landmarks` rather than inventing new facts. category "Visiting".
    These four are reference content on `/faq` only — do NOT flag any of them `homepage: true` (the homepage teaser stays the 3-4 conversion items from Task 1, D-02). Keep all answers plain prose with no HTML tags and no `</script>` substring (they will be emitted inside a JSON-LD `<script>` on /faq).
  </action>
  <acceptance_criteria>
    - `FAQ_ITEMS` now contains entries answering all seven topics: walk-ins, kids, skin-fade technique, beard-trim options, straight-razor hygiene, parking, and timing/pricing.
    - The skin-fade, beard-trim, and straight-razor answers interpolate their prices from `SERVICES` (no `$NN` literals in those answer strings).
    - The parking answer reuses the SHOP.landmarks parking facts (free lot out front / Broadmead Village / left of Starbucks).
    - No FAQ answer string contains an HTML tag or the substring `</script>`.
    - `npx tsc --noEmit` reports no type errors.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>All seven BrightLocal topics are answered in `FAQ_ITEMS` with priced answers sourced from SERVICES and plain-text (script-safe) content.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer config -> rendered HTML / JSON-LD | FAQ answer strings authored here are later emitted on `/faq` both as visible text and inside a `dangerouslySetInnerHTML` JSON-LD `<script>` (02-02). The strings are developer-controlled, not user input. No auth, no DB, no runtime input on this static-export site. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2-01 | Tampering | FAQ answer strings emitted into a JSON-LD `<script>` on /faq | mitigate | Answers are plain prose with no HTML tags and no `</script>` substring, so JSON.stringify output cannot break out of the script tag. Content is developer-authored config, not user input. |
| T-2-SC | Tampering | npm/pip/cargo installs | accept | No new dependencies are added this phase; supply-chain surface unchanged. |

No HIGH-severity threats. Realistic surface is near-zero for a content-only config change on a static export.
</threat_model>

<verification>
- `npx tsc --noEmit` passes.
- `FAQ_ITEMS` answers all seven topics; pricing answers reference SERVICES, not literals.
- At least three items flagged `homepage: true` (walk-ins, kids, timing/pricing).
</verification>

<success_criteria>
- `config.ts` is the single source of truth for FAQ content with a `FaqItem` type, category grouping field, and homepage subset flag.
- All seven BrightLocal topics answered; pricing/timing figures sourced from `SERVICES`.
</success_criteria>

<output>
Create `.planning/phases/RL-02-faq-page-with-faqpage-schema/02-01-SUMMARY.md` when done.
</output>
