---
phase: 02-faq-page-with-faqpage-schema
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified: [src/app/faq/page.tsx]
autonomous: true
requirements: [FAQ-01, FAQ-02]

must_haves:
  truths:
    - "Visiting /faq renders a dedicated page that shows answers to all seven topics, all visible in the initial HTML (no click-to-expand required) (D-03)"
    - "/faq emits exactly one FAQPage JSON-LD whose questions/answers are generated from the same FAQ_ITEMS that render visibly"
    - "/faq sets a per-page canonical of /faq and a title that does NOT re-append the shop name (title template adds it)"
  artifacts:
    - path: "src/app/faq/page.tsx"
      provides: "Static /faq server component: metadata + canonical, category-grouped all-answers-visible list, FAQPage JSON-LD"
      exports: ["metadata", "default"]
      contains: "FAQPage"
  key_links:
    - from: "src/app/faq/page.tsx"
      to: "FAQ_ITEMS"
      via: "import from @/lib/config (renders + builds JSON-LD from same source)"
      pattern: "FAQ_ITEMS"
    - from: "src/app/faq/page.tsx"
      to: "alternates.canonical"
      via: "metadata export"
      pattern: "canonical"
---

<objective>
Create the dedicated, indexable FAQ page at `src/app/faq/page.tsx` — a static server component that renders all `FAQ_ITEMS` grouped by category with every answer visible in the initial HTML (D-03), sets a per-page canonical (`/faq`) and a non-double-branded title, and emits the site's sole `FAQPage` JSON-LD (D-01) built from the same `FAQ_ITEMS` it renders (so markup matches visible content — FAQ-02).

Purpose: Delivers FAQ-01 (the page that answers all seven topics) and FAQ-02 (valid FAQPage JSON-LD + canonical). This page becomes the single owner of FAQPage structured data site-wide.
Output: `src/app/faq/page.tsx` (new). No CSS changes — reuse existing design-system classes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/RL-02-faq-page-with-faqpage-schema/02-CONTEXT.md
@.planning/phases/RL-02-faq-page-with-faqpage-schema/02-PATTERNS.md
@src/app/services/page.tsx
@src/app/layout.tsx

<interfaces>
<!-- Contract produced by plan 02-01 (config.ts). Use directly — no exploration needed. -->
FaqItem (from @/lib/config):
  { question: string; answer: string; category: string; homepage?: boolean }
FAQ_ITEMS: FaqItem[]  — already covers all seven BrightLocal topics, each with a category.

Metadata + canonical pattern to mirror — src/app/services/page.tsx lines 4-9:
  export const metadata: Metadata = { title, description, alternates: { canonical: "/services" } }
Title template (src/app/layout.tsx lines 35-37): `%s | Royal Look Barber Shop` is appended automatically,
so the page `title` must NOT contain "Royal Look Barber Shop".
JSON-LD emission pattern — <script type="application/ld+json" dangerouslySetInnerHTML={{ __html: JSON.stringify(obj) }} />.
Section shell — <section className="section"><div className="container"><div className="section-head">…</div>…</div></section>.
Reusable classes: section, container, section-head, eyebrow, serif, lede, faq-list, faq-item, faq-a p.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create the static, category-grouped /faq page with metadata + canonical</name>
  <read_first>
    - src/app/services/page.tsx (closest analog — `metadata` export with `alternates.canonical`, the section/container/section-head shell, and inline-styled answer paragraphs in SERVICE_DETAILS)
    - src/app/layout.tsx (title template `%s | Royal Look Barber Shop` — do not double-brand; metadataBase makes a relative canonical resolve to absolute)
    - src/lib/config.ts (the `FaqItem` shape + categories produced by 02-01)
    - src/app/globals.css (~lines 60-77 `.serif`/`.eyebrow`, ~lines 317-349 `.section`/`.section-head`/`.lede`, ~lines 931-988 `.faq-*` — to know which classes exist and that `.faq-a` collapses by default)
  </read_first>
  <files>src/app/faq/page.tsx</files>
  <action>
    Create `src/app/faq/page.tsx` as a server component (NO `"use client"`). Import `type { Metadata }` from `next` and `{ FAQ_ITEMS }` (and the `FaqItem` type if needed) from `@/lib/config`.

    Export `const metadata: Metadata` mirroring services/page.tsx: a `title` WITHOUT the shop name (e.g. "Barber FAQ — Victoria, BC" — exact copy is your discretion, but it must not contain "Royal Look Barber Shop"), a 1-2 sentence `en_CA` `description` summarizing the FAQ topics, and `alternates: { canonical: "/faq" }`.

    Render the `section` > `container` > `section-head` shell (h1 with `.serif`, an `.eyebrow` label, and a `.lede`). Then render the FAQ grouped by `category`: derive the ordered, unique category list from `FAQ_ITEMS` (or define an explicit category order — your discretion), and under each category heading render every item whose `category` matches. CRITICAL (D-03): all answers must be visible in the initial HTML — do NOT use the collapsing `.faq-a` accordion (it sets `max-height: 0`). Render each question as a heading (e.g. `<h3 className="serif">`) and each answer as a plain `<p>` styled to match `.faq-a p` (muted color, ~15-16px, line-height ~1.65, max-width ~70ch) using inline styles like services/page.tsx does — so NO new CSS is required. Reuse `.faq-list`/`.faq-item` wrappers and `.eyebrow` for category headings. Do NOT modify `globals.css`.
  </action>
  <acceptance_criteria>
    - `src/app/faq/page.tsx` exists, has no `"use client"` directive, and exports both `metadata` and a default component.
    - `metadata` contains `alternates: { canonical: "/faq" }` and a `title` that does NOT contain the string "Royal Look".
    - Every `FAQ_ITEMS` entry renders under a category heading with its answer in a plain (non-collapsing) element — no `.faq-a` wrapper used for answers.
    - The page imports `FAQ_ITEMS` from `@/lib/config` (content is config-driven, not inlined JSX literals).
    - `npx tsc --noEmit` reports no type errors.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>`/faq` is a static server component rendering all seven topics grouped by category, all answers visible, with canonical `/faq` and a non-double-branded title.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Emit the sole FAQPage JSON-LD from FAQ_ITEMS</name>
  <read_first>
    - src/components/FAQ.tsx (the current `faqJsonLd` object at lines 6-17 — the FAQPage shape to move onto /faq; this same block is being REMOVED from FAQ.tsx in plan 02-03 so there is exactly one FAQPage site-wide per D-01)
    - src/app/layout.tsx (lines 67-121 — how a JSON-LD `<script>` is emitted with `JSON.stringify`)
    - src/app/faq/page.tsx (the page created in Task 1)
  </read_first>
  <files>src/app/faq/page.tsx</files>
  <action>
    In `faq/page.tsx`, build a `FAQPage` JSON-LD object exactly like the one currently in `FAQ.tsx`: `@context` `https://schema.org`, `@type` `FAQPage`, and `mainEntity` mapped from `FAQ_ITEMS` to `Question` nodes (`name` = item.question) each with an `acceptedAnswer` `Answer` (`text` = item.answer). Build it from the SAME `FAQ_ITEMS` the page renders so the markup matches the visible content (FAQ-02). Emit it as a `<script type="application/ld+json" dangerouslySetInnerHTML={{ __html: JSON.stringify(faqJsonLd) }} />` inside the page's returned JSX (top of the `section` is fine, mirroring how FAQ.tsx placed it). This is the ONLY FAQPage block on the site (D-01) — the homepage copy is removed in 02-03.
  </action>
  <acceptance_criteria>
    - `src/app/faq/page.tsx` contains exactly one `application/ld+json` script whose object has `"@type"` `FAQPage`.
    - `mainEntity` is generated by mapping over `FAQ_ITEMS` (Question/acceptedAnswer/Answer), so its entries equal the rendered questions/answers one-for-one.
    - The JSON-LD object is built from `FAQ_ITEMS` (not a hand-duplicated literal list).
    - `npx tsc --noEmit` reports no type errors.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && grep -q 'FAQPage' src/app/faq/page.tsx && grep -q 'application/ld+json' src/app/faq/page.tsx</automated>
  </verify>
  <done>`/faq` emits a single FAQPage JSON-LD generated from FAQ_ITEMS that mirrors the visible Q&A.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer config -> JSON-LD `<script>` (dangerouslySetInnerHTML) | `/faq` injects a JSON-LD `<script>` via `dangerouslySetInnerHTML` from `JSON.stringify(FAQ_ITEMS-derived object)`. Input is developer-authored config (02-01), not user/runtime input. Static export: no server runtime, no auth, no DB, no query params consumed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2-02 | Tampering | FAQPage JSON-LD via dangerouslySetInnerHTML on /faq | mitigate | Object is serialized with `JSON.stringify`; source strings (02-01) are plain prose with no HTML tags or `</script>` substring, so no script-tag breakout is possible. No user input reaches this sink. |
| T-2-03 | Information Disclosure | /faq page content | accept | Page exposes only public business FAQ content (prices, hours, parking) already public elsewhere; no secrets or PII. |
| T-2-SC | Tampering | npm/pip/cargo installs | accept | No new dependencies added this phase; supply-chain surface unchanged. |

No HIGH-severity threats. Static content page with a developer-controlled JSON-LD sink.
</threat_model>

<verification>
- `npx tsc --noEmit` passes.
- `/faq` page exports `metadata` with `alternates.canonical: "/faq"` and a non-double-branded title.
- Exactly one FAQPage JSON-LD, generated from FAQ_ITEMS, mirrors the visible all-answers-shown content.
</verification>

<success_criteria>
- A static, indexable `/faq` answering all seven topics with answers visible in initial HTML.
- Valid FAQPage JSON-LD + per-page canonical, both driven from config.
</success_criteria>

<output>
Create `.planning/phases/RL-02-faq-page-with-faqpage-schema/02-02-SUMMARY.md` when done.
</output>
