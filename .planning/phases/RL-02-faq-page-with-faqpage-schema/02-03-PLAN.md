---
phase: 02-faq-page-with-faqpage-schema
plan: 03
type: execute
wave: 2
depends_on: [02-01]
files_modified: [src/components/FAQ.tsx, src/components/Footer.tsx, src/app/sitemap.ts]
autonomous: true
requirements: [FAQ-03, FAQ-04]

must_haves:
  truths:
    - "The homepage FAQ shows only the curated 3-4 question subset (FAQ_ITEMS where homepage is true), not the full list (D-02)"
    - "The homepage FAQ no longer emits any FAQPage / ld+json (so /faq is the sole owner of FAQPage — D-01)"
    - "The homepage teaser links to /faq ('See all FAQs'), the footer Pages column links /faq, and sitemap.ts includes a /faq entry — while the header nav is unchanged (D-05)"
  artifacts:
    - path: "src/components/FAQ.tsx"
      provides: "Trimmed visual-only teaser (homepage subset, no JSON-LD) + 'See all FAQs' link"
      contains: "/faq"
    - path: "src/components/Footer.tsx"
      provides: "Footer Pages-column link to /faq"
      contains: "/faq"
    - path: "src/app/sitemap.ts"
      provides: "/faq sitemap entry (priority 0.7)"
      contains: "/faq"
  key_links:
    - from: "src/components/FAQ.tsx"
      to: "FAQ_ITEMS homepage subset"
      via: "filter on the homepage flag"
      pattern: "homepage"
    - from: "src/components/FAQ.tsx"
      to: "/faq"
      via: "See all FAQs anchor"
      pattern: "/faq"
---

<objective>
Wire `/faq` into the site shell and trim the homepage FAQ to a curated teaser. In `FAQ.tsx`: remove the `faqJsonLd` const and its `<script>` (D-01 — `/faq` is now the sole FAQPage owner), filter the rendered items to the homepage subset (D-02), and add a "See all FAQs" link to `/faq` (D-05). In `Footer.tsx`: add a `/faq` link to the Pages column. In `sitemap.ts`: add the `/faq` entry. The header nav stays untouched (D-05).

Purpose: Delivers FAQ-03 (homepage shows a curated subset, not a byte-identical duplicate; FAQPage de-duplicated) and FAQ-04 (linked from footer + added to sitemap).
Output: Edits to `src/components/FAQ.tsx`, `src/components/Footer.tsx`, `src/app/sitemap.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/RL-02-faq-page-with-faqpage-schema/02-CONTEXT.md
@.planning/phases/RL-02-faq-page-with-faqpage-schema/02-PATTERNS.md
@src/components/FAQ.tsx
@src/components/Footer.tsx
@src/app/sitemap.ts

<interfaces>
<!-- Contract from plan 02-01 (config.ts). -->
FAQ_ITEMS: FaqItem[] where FaqItem = { question; answer; category; homepage?: boolean }
Homepage subset = FAQ_ITEMS.filter((item) => item.homepage) — 3-4 items (walk-ins, kids, timing/pricing, maybe one more).

Footer Pages column to extend — src/components/Footer.tsx lines 30-43 (a <ul> of <li><a href> entries).
Sitemap array to extend — src/app/sitemap.ts lines 8-15 (objects: { url, lastModified, changeFrequency, priority }).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Trim FAQ.tsx to the homepage subset and remove its FAQPage JSON-LD</name>
  <read_first>
    - src/components/FAQ.tsx (the file being modified — `faqJsonLd` const lines 6-17, the `<script>` lines 24-27, the `FAQ_ITEMS.map` accordion lines 38-58)
    - src/lib/config.ts (the `homepage` flag added in 02-01 — the subset selector)
    - .planning/phases/RL-02-faq-page-with-faqpage-schema/02-CONTEXT.md (D-01 sole FAQPage owner, D-02 curated subset)
  </read_first>
  <files>src/components/FAQ.tsx</files>
  <action>
    Delete the `faqJsonLd` const (lines ~6-17) AND the `<script type="application/ld+json" …>` element (lines ~24-27) entirely — after this change FAQ.tsx emits zero structured data (D-01; `/faq` from 02-02 is the sole FAQPage). Keep the `"use client"` directive, the `useState` accordion behaviour, and all `faq-*` markup. Change the rendered set from the full `FAQ_ITEMS` to the homepage subset by filtering on the flag (e.g. `FAQ_ITEMS.filter((item) => item.homepage)`); render that filtered array with the existing accordion. Keep the heading copy ("Good to know."). Do not import anything new beyond what's already imported (`FAQ_ITEMS` is already imported).
  </action>
  <acceptance_criteria>
    - `src/components/FAQ.tsx` contains zero `application/ld+json` scripts and no `faqJsonLd` identifier after the change.
    - The component renders `FAQ_ITEMS.filter((item) => item.homepage)` (the 3-4 curated subset), not the full array.
    - `"use client"`, the accordion `useState`, and `faq-*` markup are retained.
    - `npx tsc --noEmit` reports no type errors.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && ! grep -q 'application/ld+json' src/components/FAQ.tsx && grep -q 'homepage' src/components/FAQ.tsx</automated>
  </verify>
  <done>Homepage FAQ is a visual-only curated teaser (no JSON-LD), rendering the homepage-flagged subset.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add the "See all FAQs" link to the homepage teaser</name>
  <read_first>
    - src/components/FAQ.tsx (after Task 1 — the `.faq-list` div and the surrounding `.container`)
    - src/app/location/page.tsx (lines 119-128 — the existing CTA anchor pattern for reference; FAQ.tsx uses plain CSS, not Tailwind utility classes, so keep the link simple/inline)
    - .planning/phases/RL-02-faq-page-with-faqpage-schema/02-PATTERNS.md (FAQ.tsx section — the "See all FAQs" link guidance)
  </read_first>
  <files>src/components/FAQ.tsx</files>
  <action>
    Below the `.faq-list` div (still inside `.container`), add a single anchor linking to `/faq` with text like "See all FAQs" (an arrow such as "→" is fine). Center it with a small top margin using inline style (this component uses plain CSS, not Tailwind utilities — do NOT add Tailwind classes). This satisfies the homepage half of D-05 and gives the curated teaser a path to the full page.
  </action>
  <acceptance_criteria>
    - `src/components/FAQ.tsx` contains an anchor with `href="/faq"` and visible "See all FAQs" text.
    - The link sits below the `.faq-list` within the existing `.container`.
    - No Tailwind utility classes are introduced in FAQ.tsx.
    - `npx tsc --noEmit` reports no type errors.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && grep -q 'href="/faq"' src/components/FAQ.tsx</automated>
  </verify>
  <done>The homepage teaser links to the full `/faq` page.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Link /faq from the footer and add it to the sitemap</name>
  <read_first>
    - src/components/Footer.tsx (the "Pages" column `<ul>` lines 30-43 — Menu / Barbers / Location entries)
    - src/app/sitemap.ts (lines 6-16 — the manual route array; `/location` uses priority 0.7)
    - .planning/phases/RL-02-faq-page-with-faqpage-schema/02-CONTEXT.md (D-05 — footer link + sitemap entry; header nav stays unchanged)
  </read_first>
  <files>src/components/Footer.tsx, src/app/sitemap.ts</files>
  <action>
    In `Footer.tsx`, add one `<li><a href="/faq">FAQ</a></li>` to the existing "Pages" column `<ul>` (alongside Menu / Barbers / Location). Make no other Footer changes.

    In `sitemap.ts`, add one entry to the returned array mirroring the existing objects: `url` = `${base}/faq`, `lastModified: new Date()`, `changeFrequency: "monthly"`, `priority: 0.7` (matching `/location`, an informational page).

    Do NOT touch the header nav: do not modify `src/components/Navbar.tsx` or the `NAV_LINKS_LEFT`/`NAV_LINKS_RIGHT` arrays in config (D-05 — keep header nav clean/unchanged).
  </action>
  <acceptance_criteria>
    - `src/components/Footer.tsx` contains a Pages-column `<li>` with `href="/faq"` and text "FAQ".
    - `src/app/sitemap.ts` returns an entry whose `url` ends with `/faq` and `priority` 0.7.
    - No changes to `Navbar.tsx` or the `NAV_LINKS_*` arrays (header nav unchanged).
    - `npx tsc --noEmit` reports no type errors.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && grep -q 'href="/faq"' src/components/Footer.tsx && grep -q '/faq' src/app/sitemap.ts</automated>
  </verify>
  <done>`/faq` is reachable from the footer and present in the generated sitemap; header nav untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| structured-data integrity | Removing the homepage's FAQPage JSON-LD must leave exactly one FAQPage site-wide (on /faq). Mis-removal would either leave two FAQPage blocks (ambiguous structured data) or drop it entirely. All edits are developer-controlled markup; no user/runtime input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2-04 | Repudiation / data integrity | FAQPage de-duplication across homepage + /faq | mitigate | FAQ.tsx is asserted to contain zero `application/ld+json` after the change; the build-verification plan (02-04) confirms the built homepage HTML has no FAQPage and /faq has exactly one. |
| T-2-05 | Information Disclosure | footer/sitemap links | accept | Links expose only the public `/faq` route; no secrets or PII. Header nav intentionally left unchanged. |
| T-2-SC | Tampering | npm/pip/cargo installs | accept | No new dependencies added this phase; supply-chain surface unchanged. |

No HIGH-severity threats. Markup-only edits on a static export.
</threat_model>

<verification>
- `npx tsc --noEmit` passes.
- FAQ.tsx: zero `application/ld+json`, renders the `homepage` subset, has a `/faq` link.
- Footer.tsx links `/faq`; sitemap.ts includes a `/faq` entry at priority 0.7.
- Navbar / NAV_LINKS unchanged.
</verification>

<success_criteria>
- Homepage FAQ is a curated, schema-free teaser linking to `/faq`; `/faq` is the sole FAQPage owner.
- `/faq` is linked from the footer and present in the sitemap; header nav is unchanged.
</success_criteria>

<output>
Create `.planning/phases/RL-02-faq-page-with-faqpage-schema/02-03-SUMMARY.md` when done.
</output>
