---
phase: 02-faq-page-with-faqpage-schema
plan: 04
type: execute
wave: 3
depends_on: [02-01, 02-02, 02-03]
files_modified: []
autonomous: false
requirements: [FAQ-02, FAQ-04]

must_haves:
  truths:
    - "npm run build completes a clean static export that includes out/faq/index.html"
    - "out/faq/index.html shows all seven topics, carries an absolute canonical to /faq, and contains exactly one FAQPage JSON-LD; the built homepage contains zero FAQPage"
    - "Pricing answers in the built /faq HTML show the live SERVICES figures; the footer links /faq and the sitemap lists /faq"
    - "The /faq FAQPage JSON-LD validates with no errors in an external validator and its Q&A match the visible content"
  artifacts:
    - path: "out/faq/index.html"
      provides: "Rendered, indexable FAQ page in the static export"
      contains: "FAQPage"
  key_links:
    - from: "out/faq/index.html"
      to: "SERVICES pricing figures"
      via: "build-time interpolation"
      pattern: "\\$25"
    - from: "out/faq/index.html"
      to: "schema.org FAQPage"
      via: "application/ld+json script validated externally"
      pattern: "application/ld\\+json"
---

<objective>
Final phase gate: run the full static build and assert every Phase 2 requirement against the generated `out/` output, then collect human sign-off on the one manual-only check the build cannot decide — external FAQPage JSON-LD validity plus a non-duplication read of the homepage teaser vs `/faq`.

Purpose: Closes FAQ-04 (clean static export including `/faq`, linked from footer + in sitemap) and the validity half of FAQ-02 (FAQPage JSON-LD valid and matching visible content). Confirms D-01 (exactly one FAQPage site-wide) holds in the built output.
Output: A green full build, a passing assertion log, and recorded human approval (no source files changed).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/RL-02-faq-page-with-faqpage-schema/02-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Full build + per-requirement assertions over out/</name>
  <read_first>
    - .planning/REQUIREMENTS.md (FAQ-01..FAQ-04 — the assertion source of truth)
    - .planning/ROADMAP.md (Phase 2 Success Criteria 1-4)
    - src/lib/config.ts (confirm the live SERVICES figures the /faq answers should show — Kids $25, Skin Fade $30, Trim Beard $20, Hot Shave $35; and the parking facts referencing "Starbucks")
  </read_first>
  <files>(no source changes — verification only)</files>
  <action>
    Run `npm run build` from the repo root and confirm it exits 0 (clean static export to `out/`). Then assert the Phase 2 requirements against the generated output and record pass/fail for each:
    - FAQ-04 (route built): `out/faq/index.html` exists.
    - FAQ-01 (topics present): the built `/faq` HTML shows the topic content — confirm the parking topic (token `Starbucks` from SHOP.landmarks) and at least one SERVICES-sourced price (`$25` kids) are present; spot-check that walk-ins, fade, beard, and shave content render too.
    - FAQ-02 (canonical): `out/faq/index.html` contains `rel="canonical" href="https://royallook.ca/faq"`.
    - FAQ-02 / D-01 (single FAQPage): `out/faq/index.html` contains exactly ONE occurrence of `FAQPage`, and `out/index.html` (homepage) contains ZERO occurrences of `FAQPage` (the homepage still carries the site-wide `BarberShop` JSON-LD from layout — that is expected and must not be confused with FAQPage).
    - FAQ-04 (linked): `out/index.html` contains `href="/faq"` (homepage "See all FAQs" + footer link), and `out/sitemap.xml` contains `royallook.ca/faq`.
    Report any failing assertion as a blocker — do not approve the phase with a red assertion.
  </action>
  <acceptance_criteria>
    - `npm run build` exits 0.
    - `out/faq/index.html` exists, contains `rel="canonical" href="https://royallook.ca/faq"`, the `Starbucks` parking token, and the `$25` SERVICES price.
    - `out/faq/index.html` contains exactly one `FAQPage` occurrence; `out/index.html` contains zero.
    - `out/index.html` contains `href="/faq"`; `out/sitemap.xml` contains `royallook.ca/faq`.
  </acceptance_criteria>
  <verify>
    <automated>npm run build && test -f out/faq/index.html && grep -q 'rel="canonical" href="https://royallook.ca/faq"' out/faq/index.html && grep -q 'Starbucks' out/faq/index.html && grep -q '\$25' out/faq/index.html && [ "$(grep -o 'FAQPage' out/faq/index.html | wc -l)" -eq 1 ] && ! grep -q 'FAQPage' out/index.html && grep -q 'href="/faq"' out/index.html && grep -q 'royallook.ca/faq' out/sitemap.xml</automated>
  </verify>
  <done>Full static export is clean, `/faq` is built/linked/in-sitemap, and exactly one FAQPage exists site-wide; any failure is surfaced as a blocker before sign-off.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human sign-off — external FAQPage validity + homepage non-duplication</name>
  <read_first>
    - out/faq/index.html (the `<script type="application/ld+json">` FAQPage block to validate, and the visible Q&A to compare against it)
    - out/index.html (the homepage teaser — to confirm it shows only the curated subset and no FAQPage)
    - .planning/ROADMAP.md (Phase 2 Success Criteria + the rich-results caveat: do NOT expect a Rich Results accordion preview — Google restricts FAQ rich results to gov/health since 2023; structural validity is what matters)
  </read_first>
  <files>(no source changes — manual sign-off only)</files>
  <what-built>
    A dedicated `/faq` page built to `out/faq/index.html` emitting a single `FAQPage` JSON-LD generated from `FAQ_ITEMS`, plus a trimmed homepage teaser (curated subset, no FAQPage) that links to `/faq`. Task 1's greppable assertions already passed; the checks below require human/external judgment.
  </what-built>
  <action>
    Present the manual-only checks to the user and collect approval (this task writes no source). (1) FAQPage validity (FAQ-02): from `out/faq/index.html` copy the `<script type="application/ld+json">` contents and paste into https://validator.schema.org; expect zero errors and a single `FAQPage` with `Question`/`acceptedAnswer` nodes whose text matches the page's visible questions/answers one-for-one. Do not expect a Rich Results accordion preview (restricted to gov/health). (2) Non-duplication + voice (FAQ-03 / D-02): compare the homepage teaser (`out/index.html`) against `/faq` — confirm the homepage shows only the curated 3-4 subset (walk-ins, kids, timing/pricing focus), is clearly not a byte-identical duplicate of the full `/faq`, and that the seven `/faq` answers read as genuinely useful, warm, neighbourhood `en_CA` copy (not one-liners or keyword stuffing). If either check fails, record the specific JSON-LD error or the answer needing a rewrite so a gap-closure pass can fix it rather than approving.
  </action>
  <how-to-verify>
    1. FAQPage validity (FAQ-02): Open `out/faq/index.html`, copy the `<script type="application/ld+json">…</script>` contents, paste into https://validator.schema.org. Expect zero errors; one `FAQPage`; each `Question`/`acceptedAnswer` text matches a visible Q&A on the page. (No Rich Results preview is expected — see the roadmap caveat.)
    2. Non-duplication + voice (FAQ-03): View the homepage FAQ and `/faq`. Confirm the homepage shows only the curated 3-4 subset and is not a byte-identical duplicate of `/faq`, and that all seven `/faq` answers read as genuinely useful 1-3 sentence answers in the established warm `en_CA` voice.
    Approve only if both checks pass; otherwise describe the specific JSON-LD error or the answer that needs a rewrite.
  </how-to-verify>
  <verify>
    <human-check>External validator reports zero errors for the /faq FAQPage JSON-LD and its Q&A match the visible content; the homepage teaser is confirmed a non-duplicate curated subset and the seven answers read as genuine local copy. Automated build-output assertions are covered by Task 1.</human-check>
  </verify>
  <acceptance_criteria>
    - validator.schema.org reports zero errors for the /faq FAQPage JSON-LD.
    - The JSON-LD Question/Answer text matches the visible /faq Q&A one-for-one.
    - The homepage teaser is confirmed a curated subset, not a byte-identical duplicate of /faq.
    - Human approval recorded (or specific issues listed for follow-up).
  </acceptance_criteria>
  <done>The /faq FAQPage validates cleanly externally and matches visible content, and the homepage teaser is confirmed a non-duplicate subset; user approval recorded (or issues logged for gap closure).</done>
  <resume-signal>Type "approved" once the FAQPage validates with no errors and the homepage teaser is confirmed a non-duplicate curated subset, or describe the issues to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| verification only | This plan changes no source; it inspects build output and consults an external schema validator. No new attack surface is introduced. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2-06 | (validation gate) | /faq FAQPage external validation | mitigate | Human checkpoint blocks phase completion until schema.org validation is clean and the JSON-LD matches visible content — catches malformed structured data before it ships. |
| T-2-07 | Repudiation / data integrity | single-FAQPage invariant | mitigate | Task 1 asserts exactly one FAQPage in built `/faq` HTML and zero in the built homepage, enforcing D-01 in the shipped output. |
| T-2-SC | Tampering | npm/pip/cargo installs | accept | No package installs this phase; supply-chain surface unchanged. |

No HIGH-severity threats. The only control here is the verification gate itself; realistic threat surface is near-zero for a static content page.
</threat_model>

<verification>
- `npm run build` exits 0 and `out/faq/index.html` exists.
- Exactly one FAQPage in built `/faq`, zero on the built homepage; absolute canonical to `/faq`.
- `/faq` linked from footer + present in `out/sitemap.xml`; SERVICES prices visible in built `/faq`.
- FAQPage validates externally with no errors and matches the visible Q&A (human).
</verification>

<success_criteria>
- Clean static export including `/faq`, linked from footer and in the sitemap.
- Valid FAQPage JSON-LD matching visible content, with exactly one FAQPage site-wide.
- Phase 2 is ready for `/gsd:verify-work`.
</success_criteria>

<output>
Create `.planning/phases/RL-02-faq-page-with-faqpage-schema/02-04-SUMMARY.md` when done.
</output>
