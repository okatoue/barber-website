---
phase: 03-gbp-services-alignment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [.planning/phases/RL-03-gbp-services-alignment/03-GBP-ALIGNMENT.md]
autonomous: true
requirements: [GBP-01, GBP-02, GBP-03, GBP-04]

must_haves:
  truths:
    - "The paste-ready deliverable 03-GBP-ALIGNMENT.md exists in the phase folder as the single repo artifact for this phase (D-06)"
    - "Deliverable documents the current GBP state — primary category Barber shop, no secondary category, Services section empty (link-only to royallook.ca/services), exact-match NAP, 4.9 / ~122 reviews, plus the auto-extracted review topics (GBP-01)"
    - "Deliverable contains a gap table reconciling all 8 website SERVICES against GBP, flagging the empty Services section and the GBP-vs-site name differences (GBP-02)"
    - "Deliverable lists all 8 services as paste-ready GBP entries — Skin Fade, Classic Men's Cut, Kids' Cut, Hot-Towel Straight-Razor Shave, Beard Trim or Lineup, Buzz Cut, Senior's Cut, Hair Wash — each with a name, a short plain-English description, and a price (D-02, GBP-03)"
    - "No GBP service description contains a phone number or a URL (Google spam rule) (GBP-03)"
    - "Eyebrow trim is documented as deliberately excluded — not offered as a standalone service, no SERVICES change (D-03)"
    - "Deliverable records GBP-04 as resolved with no config.ts site renames — the descriptive GBP names live only in the dashboard entries, the site keeps Trim Beard / Hot Shave (D-01)"
    - "Deliverable documents the primary-only secondary-category choice as deliberate (D-05) and the untouched googleReviewCount offline-fallback (live count auto-pulled at build) as no-change (D-04)"
  artifacts:
    - path: ".planning/phases/RL-03-gbp-services-alignment/03-GBP-ALIGNMENT.md"
      provides: "Current GBP state writeup, GBP-vs-site gap table, 8 paste-ready GBP service entries (name/description/price), and the D-01/D-03/D-04/D-05 decision notes plus owner action checklist"
      contains: "Description:"
      min_lines: 70
  key_links:
    - from: ".planning/phases/RL-03-gbp-services-alignment/03-GBP-ALIGNMENT.md gap table + 8 entries"
      to: "src/lib/config.ts SERVICES"
      via: "name + price reconciliation (all 8 menu items represented)"
      pattern: "Skin Fade|Regular Hair Cut|Kids|Senior|Buzz Cut|Trim Beard|Hot Shave|Hair Wash"
---

<objective>
Produce the single repo deliverable for Phase 3: a paste-ready `03-GBP-ALIGNMENT.md` that (a) documents the current Google Business Profile state, (b) reconciles GBP against the website `SERVICES` menu in a gap table, (c) gives all 8 services as paste-ready GBP Services entries (name, plain-English description, price; no phone numbers, no URLs), and (d) records the deliberate "no code change" decisions (GBP-04 no-rename, primary-only category, untouched review count, eyebrow-trim excluded).

Purpose: BrightLocal flagged that the GBP Services section is empty. This deliverable is the clean, copy-from artifact the owner pastes into the GBP dashboard — closing GBP-01..GBP-04. RESEARCH.md stays the working notes; this is the finished hand-off.

Output: `.planning/phases/RL-03-gbp-services-alignment/03-GBP-ALIGNMENT.md` (the only repo write this phase makes — no application code is touched).

This is a documentation-only phase. There are NO code edits: `src/lib/config.ts` (SERVICES, googleReviewCount) and all components stay exactly as-is per D-01 and D-04. Do not create config or component tasks.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/RL-03-gbp-services-alignment/03-CONTEXT.md
@.planning/phases/RL-03-gbp-services-alignment/RESEARCH.md
@src/lib/config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the paste-ready GBP-ALIGNMENT deliverable</name>
  <files>.planning/phases/RL-03-gbp-services-alignment/03-GBP-ALIGNMENT.md</files>
  <read_first>
    - .planning/phases/RL-03-gbp-services-alignment/03-CONTEXT.md (AUTHORITATIVE — the six locked decisions D-01..D-06; read this first and most carefully)
    - .planning/phases/RL-03-gbp-services-alignment/RESEARCH.md (the audit: §1 current GBP state, §2 website menu, §3 the 5 drafted descriptions, §4 owner decisions, §5 dashboard attributes to confirm)
    - src/lib/config.ts (READ-ONLY — pull the exact 8 SERVICES names + prices and the SHOP NAP; do NOT edit this file)
    - .planning/REQUIREMENTS.md (GBP-01..GBP-04 acceptance wording)
  </read_first>
  <action>
    Create the deliverable at `.planning/phases/RL-03-gbp-services-alignment/03-GBP-ALIGNMENT.md` with these sections, all in warm, plain-English `en_CA` brand voice (no hype):

    1. Current GBP state (covers GBP-01) — summarize from RESEARCH §1: primary category `Barber shop`, no secondary category visible, the Services section is EMPTY (the panel's "Services" is just a link to royallook.ca/services — net-new build, not an edit), exact-match NAP (777 Royal Oak Dr #530, Victoria, BC V8X 4V1; phone (778) 430-0040; website royallook.ca), hours opening 9 a.m., rating 4.9 across ~122 reviews, wheelchair-accessible attribute present, active owner posts. Also note Google's auto-extracted review topics (beard trim, eyebrow trim, fade, skilled barber) as demand signal. (URLs/phone here in the state writeup are fine — the no-phone/no-URL rule applies ONLY to the service description fields in section 3.)

    2. Gap table (covers GBP-02) — a markdown table with one row per the 8 website SERVICES, columns: Website service (config name) | GBP entry name | Price | Currently on GBP? | Note. All 8 rows mark "Currently on GBP?" as "No — Services section empty". The five core rows note the GBP name differs from the site name; the three extras note they round out the list. The 8 website items (from config.ts SERVICES, exact names + prices): Regular Hair Cut $28, Skin Fade $30, Kids $25, Senior $25, Buzz Cut $20, Trim Beard $20, Hot Shave $35, Hair Wash $7.

    3. Paste-ready GBP service entries (covers GBP-03, D-02) — all 8 services, each formatted as exactly three labeled lines so they are copy-across-paste-ready: a line `**Service:** <GBP name>`, a line `**Description:** <plain-English description>`, and a line `**Price:** <price>`. Hard rule: NO phone numbers and NO URLs anywhere in any Description line (Google flags them as spam). The 8 entries (GBP name ← config name, price):
       - `Skin Fade` ← Skin Fade, $30 — reuse the drafted description from RESEARCH §3.
       - `Classic Men's Cut` ← Regular Hair Cut, $28 — reuse the drafted description from RESEARCH §3.
       - `Kids' Cut` ← Kids, $25 — reuse the drafted description from RESEARCH §3.
       - `Hot-Towel Straight-Razor Shave` ← Hot Shave, $35 — reuse the drafted description from RESEARCH §3.
       - `Beard Trim or Lineup` ← Trim Beard, $20 — reuse the drafted description from RESEARCH §3.
       - `Buzz Cut` ← Buzz Cut, $20 — WRITE a new short description (single-guard all-over clipper cut taken to one length for a clean, even, low-maintenance look; quick and tidy).
       - `Senior's Cut` ← Senior, $25 — WRITE a new short description (an unhurried, comfortable cut for older clients, scissor or clipper, neatened around the ears and neck).
       - `Hair Wash` ← Hair Wash, $7 — WRITE a new short description (a quick shampoo and towel dry, on its own or added before a cut, to leave hair clean and fresh).
       Keep the three new descriptions short (roughly 1–2 sentences, within GBP's service-description length limit), plain-English, en_CA, no hype, no phone, no URL.

    4. Decision notes — record the deliberate "no code change" calls so each reads as an intentional choice, not an oversight:
       - GBP-04 / D-01 (no site renames): config.ts SERVICES names stay as-is (`Trim Beard`, `Hot Shave`, etc.); the descriptive GBP names ("Beard Trim or Lineup", "Hot-Towel Straight-Razor Shave") are used ONLY in the GBP dashboard entries above, not mirrored back into the site menu. landing.ts already surfaces "Beard Trim" via its displayName mapping. GBP-04's "renames to apply in config.ts" is resolved as "none".
       - D-03 (eyebrow trim): mentioned in Google's auto-extracted review topics but NOT offered as a standalone service → excluded from both the site menu and the GBP Services list. No SERVICES change.
       - D-05 (secondary category): leave primary-only ("Barber shop"); no strong second category fits and "Hair salon" would dilute relevance — deliberate.
       - D-04 (review count): `SHOP.googleReviewCount` (127) is the offline/local-dev fallback only; the live ~122 is auto-pulled at build time via google-reviews.ts and refreshed by the daily deploy cron, so the deployed site already shows the live count — no code change.

    5. Owner action checklist (Claude's discretion to include — recommended) — a short checklist for the owner: sign in to GBP → Edit profile → Services, add each of the 8 entries above (name + description + price), and confirm in the dashboard the unverified attributes from RESEARCH §5 (appointments/walk-ins, "Identifies as…", payment types, restroom, parking) and that Services was genuinely empty before adding.

    Do NOT paste full source-file contents into the deliverable; pull only the service names/prices and the §3 descriptions. Do NOT edit src/lib/config.ts or any component.
  </action>
  <acceptance_criteria>
    - `03-GBP-ALIGNMENT.md` exists at `.planning/phases/RL-03-gbp-services-alignment/03-GBP-ALIGNMENT.md`.
    - The file contains a "Current GBP state" section noting primary category Barber shop, no secondary category, and the Services section empty (GBP-01).
    - The file contains a gap table with a row for each of the 8 website SERVICES (Regular Hair Cut, Skin Fade, Kids, Senior, Buzz Cut, Trim Beard, Hot Shave, Hair Wash) (GBP-02).
    - The file contains exactly 8 `**Service:**` lines, 8 `**Description:**` lines, and 8 `**Price:**` lines (GBP-03, D-02).
    - The three new descriptions (Buzz Cut, Senior's Cut, Hair Wash) are present and non-empty.
    - The file contains the GBP-04 no-rename note, the D-03 eyebrow-trim exclusion, the D-05 primary-only category note, and the D-04 review-count note.
  </acceptance_criteria>
  <verify>
    <automated>F=.planning/phases/RL-03-gbp-services-alignment/03-GBP-ALIGNMENT.md; test -f "$F" && [ "$(grep -c '^\*\*Service:\*\*' "$F")" -eq 8 ] && [ "$(grep -c '^\*\*Description:\*\*' "$F")" -eq 8 ] && [ "$(grep -c '^\*\*Price:\*\*' "$F")" -eq 8 ] && echo PASS</automated>
  </verify>
  <done>The deliverable file exists with all five sections, a gap table covering all 8 services, and exactly 8 three-line GBP entries.</done>
</task>

<task type="auto">
  <name>Task 2: Self-verify the deliverable against GBP-01..04 and reconcile to config</name>
  <files>.planning/phases/RL-03-gbp-services-alignment/03-GBP-ALIGNMENT.md</files>
  <read_first>
    - .planning/phases/RL-03-gbp-services-alignment/03-GBP-ALIGNMENT.md (the deliverable just authored — read it in full)
    - src/lib/config.ts (READ-ONLY — to reconcile the 8 SERVICES names + prices against the deliverable)
    - .planning/REQUIREMENTS.md (GBP-01..GBP-04 acceptance wording — confirm each is satisfied)
  </read_first>
  <action>
    Run the no-phone / no-URL gate and the structural reconciliation against `03-GBP-ALIGNMENT.md`. The critical, highest-risk check is that NO `**Description:**` line contains a phone number or a URL (Google spam rule, GBP-03). Then reconcile: confirm every config.ts SERVICES name + price appears in the gap table, confirm all 8 GBP entries each have a name/description/price, and confirm the four decision notes (GBP-04 no-rename, D-03 eyebrow-trim, D-05 primary-only category, D-04 review count) are present. If any greppable gate fails or a service/price is missing, fix the deliverable in Task 1's file (re-author the offending section) until all gates pass — do NOT relax the no-phone/no-URL rule. Make no code changes.
  </action>
  <acceptance_criteria>
    - Grepping the `**Description:**` lines for `https?://`, `www.`, an area code in parentheses, or a 3-3-4 digit phone pattern returns ZERO matches (GBP-03 spam rule holds).
    - All 8 config.ts SERVICES names and their prices ($28, $30, $25, $25, $20, $20, $35, $7) are present in the deliverable's gap table.
    - The deliverable contains the literal requirement-traceable notes: GBP-04 resolved as no renames, eyebrow trim excluded, secondary category primary-only, review count unchanged.
    - All four requirements GBP-01, GBP-02, GBP-03, GBP-04 are demonstrably addressed by named sections of the deliverable.
  </acceptance_criteria>
  <verify>
    <automated>F=.planning/phases/RL-03-gbp-services-alignment/03-GBP-ALIGNMENT.md; grep '^\*\*Description:\*\*' "$F" | grep -Eq 'https?://|www\.|\([0-9]{3}\)|[0-9]{3}[-. ][0-9]{3}[-. ][0-9]{4}' && { echo "FAIL: phone/URL found in a description"; exit 1; }; for n in 'Skin Fade' 'Regular Hair Cut' 'Kids' 'Senior' 'Buzz Cut' 'Trim Beard' 'Hot Shave' 'Hair Wash'; do grep -Fq "$n" "$F" || { echo "FAIL: missing $n"; exit 1; }; done; grep -Eq 'primary-only|primary only' "$F" || { echo 'FAIL: primary-only note missing'; exit 1; }; grep -Eiq 'eyebrow' "$F" || { echo 'FAIL: eyebrow-trim note missing'; exit 1; }; echo PASS</automated>
  </verify>
  <done>The no-phone/no-URL gate passes on all description lines, all 8 services + prices reconcile against config.ts, and the four decision notes are present — GBP-01..04 satisfied by the deliverable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | This phase writes a single static markdown document into `.planning/`. It touches no application code, no runtime, no user input, no data flow, and adds no new attack surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | (n/a) | `03-GBP-ALIGNMENT.md` (static docs) | accept | Documentation-only change. No code executes, no input is processed, no secrets or PII involved. No HIGH or MEDIUM threats exist for this phase. |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase (zero dependency changes) — supply-chain gate is not applicable. |

No HIGH/MEDIUM threats. Off-site publishing into the GBP dashboard is owner action outside this codebase and outside this threat model.
</threat_model>

<verification>
- `.planning/phases/RL-03-gbp-services-alignment/03-GBP-ALIGNMENT.md` exists.
- Exactly 8 GBP entries, each with `**Service:**`, `**Description:**`, `**Price:**`.
- No `**Description:**` line contains a phone number or URL.
- Gap table covers all 8 config.ts SERVICES with prices.
- Decision notes present: GBP-04 no-rename (D-01), eyebrow trim excluded (D-03), primary-only category (D-05), review count unchanged (D-04).
- `git diff --name-only` shows only the new deliverable — no changes to `src/lib/config.ts` or any component (D-01/D-04 honored).
</verification>

<success_criteria>
1. Current GBP state is documented — primary/secondary categories, the empty Services section, attributes, rating/reviews (GBP-01).
2. A gap table reconciles GBP against the website `SERVICES`, flagging the empty section and name differences (GBP-02).
3. Paste-ready GBP entries exist for all 8 offerings — clear names, short plain-English descriptions with no phone numbers or URLs, prices noted (GBP-03, D-02).
4. The site↔GBP rename question is resolved and documented as "no config.ts renames" (GBP-04, D-01), alongside the primary-only category (D-05), review-count (D-04), and eyebrow-trim exclusion (D-03) notes.
5. No application code changed — the only repo write is the deliverable.
</success_criteria>

<output>
Create `.planning/phases/RL-03-gbp-services-alignment/03-01-SUMMARY.md` when done.
</output>
